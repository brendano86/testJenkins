node { // <1>
    stage('Build') {
        echo 'Starting build phase'
        sh 'npm install'
    }
    stage('Test') {
        /* .. snip .. */
    }
    stage('Deploy') {
        /* .. snip .. */
    }
}